# Proyecto Dokploy: db (Postgres)
# Cambiar POSTGRES_PASSWORD antes del primer deploy.

POSTGRES_PASSWORD=genera-una-clave-larga-aqui

# Opcional — admin Postgres desde el host (default solo localhost)
# POSTGRES_PUBLISH_BIND=127.0.0.1
# POSTGRES_ALLOWED_CIDRS=tu.ip.publica/32
