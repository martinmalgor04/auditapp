{
  "$schema": "./_ecc/schemas/ecc-install-config.schema.json",
  "version": 1,
  "target": "claude-project",
  "profile": "full",
  "include": ["lang:typescript"],
  "options": {
    "notes": "ECC full a nivel proyecto (auditapp/.claude). Stack: SvelteKit + Postgres + R2. Ajustar con /project-init."
  }
}
